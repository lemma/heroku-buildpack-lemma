#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e

sha="593150d"
url="https://assets.lemma.io/lemmad-$sha-linux-amd64"

dir="$(dirname ${BASH_SOURCE[0]})"
mkdir -p $1/.profile.d
cp $dir/lemma.sh $1/.profile.d/

echo -n "-----> Fetching lemmad coprocess..."

curl -so $1/.profile.d/lemmad $url

echo " done"

exit 0
